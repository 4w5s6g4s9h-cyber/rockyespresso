# Champions Builder

Static web app for building Pokemon Champions teams.

Open `index.html` locally, or publish the repository with GitHub Pages.

For local development, run a static server from this folder and open the app through HTTP:

```sh
python3 -m http.server 8000
```

Run the lightweight logic tests with:

```sh
node tests/team-analysis.test.mjs
```
