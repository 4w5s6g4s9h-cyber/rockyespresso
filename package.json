{
  "name": "champions-builder",
  "version": "1.0.0",
  "private": true,
  "description": "Static web app for building Pokemon Champions teams.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build-browser-bundle.mjs && node scripts/embed-local-data.mjs",
    "build:pages": "npm run build && node scripts/prepare-pages.mjs",
    "test": "node --test tests/*.test.mjs",
    "test:browser": "node scripts/browser-smoke.mjs",
    "data:sync": "node scripts/sync-smogon-pokemon.mjs && node scripts/sync-champions-learnsets.mjs && node scripts/update-smogon-movesets.mjs && npm run audit:data && npm run build",
    "audit:data": "node scripts/audit-champions-data.mjs",
    "serve": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
